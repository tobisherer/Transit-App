/* This file will gather data from Metro Transit LA and parse the
/* data for use when displaying route information */

//Get route info and arrival/departure times
void getStationInfo()
{
  // request information from API and 
  // use API calls to get stops for particular route, arrival predictions for each stop, etc
  // call parseData(data) to parse data retrieved
}

// Parse the data for use later, save strings to file for later
void parseData(data)
{
  //  convert data retrieved into string
  //  compare with previous entries
  //  if there is a change, call update_display()
  //  else, do nothing
}
